#ifndef STATE_H
# define STATE_H

/* Public libraries */
#include <stdio.h>

/* Private libraries */
#include "timestamp.h"

void eat(size_t i);
void sleep(size_t i);
void has_died(size_t i);
void think(size_t i);

#endif